if __name__ == '__main__':
    n = int(input())
    arr = map(int, input().split())
    
    # Remove duplicates using set() and sort the unique scores
    unique_scores = sorted(list(set(arr)), reverse=True)
    
    # The runner-up is the second element in the sorted list
    print(unique_scores[1])
